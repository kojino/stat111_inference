sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sum((Y.star-mean(Y.star)) ** 2) / (n - 1)
cat("variance of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y_star,size=n,replace=T)
bootstrap.stddev <- sqrt(sum((bootstrap-mean(bootstrap)) ** 2) / (n - 1))
bootstrap.stddev
}
bootsrap.stddevs <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",mean(bootsrap.stddevs))
bootsrap.stddevs <- replicate(R, calc_bootsrap_stddev(80))
cat("variance of the sample mean (bootstrap):",mean(bootsrap.stddevs))
bootsrap.stddevs <- replicate(R, calc_bootsrap_stddev(80))
cat("variance of the sample mean (bootstrap):",mean(bootsrap.stddevs))
bootsrap.stddevs <- replicate(R, calc_bootsrap_stddev(80))
cat("variance of the sample mean (bootstrap):",mean(bootsrap.stddevs))
bootsrap.stddevs <- replicate(R, calc_bootsrap_stddev(80))
cat("variance of the sample mean (bootstrap):",mean(bootsrap.stddevs))
bootsrap.stddevs <- replicate(R, calc_bootsrap_stddev(80))
cat("variance of the sample mean (bootstrap):",mean(bootsrap.stddevs))
bootsrap.stddevs <- replicate(R, calc_bootsrap_stddev(80))
cat("variance of the sample mean (bootstrap):",mean(bootsrap.stddevs))
bootsrap.stddevs <- replicate(R, calc_bootsrap_stddev(80))
cat("variance of the sample mean (bootstrap):",mean(bootsrap.stddevs))
bootsrap.stddevs <- replicate(R, calc_bootsrap_stddev(80))
cat("variance of the sample mean (bootstrap):",mean(bootsrap.stddevs))
bootsrap.stddevs <- replicate(R, calc_bootsrap_stddev(80))
cat("variance of the sample mean (bootstrap):",mean(bootsrap.stddevs))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y_star,size=n,replace=T)
bootstrap.stddev <- sqrt(sum((bootstrap-mean(bootstrap)) ** 2) / (n - 1))
bootstrap.stddev
}
bootsrap.stddevs <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",mean(bootsrap.stddevs))
n    <- 7
size <- 40
prob <- 0.1
samples <- rbinom(n, size, prob)
ecdf(samples)
plot(ecdf(samples))
curve(pbinom(n, size, prob))
pbinom(n, size, prob)
curve(pbinom(n, size, prob),add = True)
curve(pbinom(n, size, prob),add = TRUE)
x <- rbinom(10000)
curve(pbinom(x, size, prob),add = TRUE)
curve(dbinom(x, size, prob),add = TRUE)
x
x <- rbinom(10000)
x <- rbinom(10000, size, prob)
x
curve(dbinom(x, size, prob),add = TRUE)
curve(pbinom(x, size, prob),add = TRUE)
curve(qbinom(x, size, prob),add = TRUE)
plot(ecdf(samples))
curve(pbinom(x, size, prob),add = TRUE)
plot(ecdf(samples))
ecdfs <- replicate(R, {
samples <- rbinom(n, size, prob)
ecdf(samples)
}}
ecdfs <- replicate(100, {
samples <- rbinom(n, 40, prob)
ecdf(samples)
})
ecdf.2s <- replicate(100, {
samples <- rbinom(n, 40, prob)
ecdf(samples)(2)
})
ecdf.2s
sd(ecdf.2s)
cdf.2 <- dbinom(0, size=size, prob=prob) +
+ dbinom(1, size=size, prob=prob) +
+ dbinom(2, size=size, prob=prob)
cdf.2 * (1 - cdf.2) /40
cdf.2
sqrt(cdf.2 * (1 - cdf.2) /40)
ecdf.2s <- replicate(100, {
samples <- rbinom(n, 40, prob)
ecdf(samples)(2)
})
sd(ecdf.2s)
ecdf.2s <- replicate(100, {
samples <- rbinom(n, 40, prob)
ecdf(samples)(2)
})
sd(ecdf.2s)
ecdf.2s <- replicate(100, {
samples <- rbinom(n, 40, prob)
ecdf(samples)(2)
})
sd(ecdf.2s)
ecdf.2s <- replicate(100, {
samples <- rbinom(n, 40, prob)
ecdf(samples)(2)
})
sd(ecdf.2s)
ecdf.2s <- replicate(100, {
samples <- rbinom(n, 40, prob)
ecdf(samples)(2)
})
sd(ecdf.2s)
ecdf.2s <- replicate(100, {
samples <- rbinom(n, 40, prob)
ecdf(samples)(2)
})
sd(ecdf.2s)
ecdf.2s <- replicate(100, {
samples <- rbinom(n, 40, prob)
ecdf(samples)(2)
})
sd(ecdf.2s)
ecdf.2s <- replicate(100, {
samples <- rbinom(n, 40, prob)
ecdf(samples)(2)
})
sd(ecdf.2s)
ecdf.2s <- replicate(100, {
samples <- rbinom(n, 40, prob)
ecdf(samples)(2)
})
sd(ecdf.2s)
df         <- read.csv(file="baseball.csv", header=TRUE, sep=",")
K          <- nrow(df)
mean(df$Salary)
K
df
pop.mean   <- mean(df$Salary)
sqrt(sum((df$Salary-mean(df$Salary))**2) / nrow(vec))
calc_pop_sd <- function (vec) {
sqrt(sum((df$Salary-mean(df$Salary))**2) / nrow(vec))
}
pop.mean   <- mean(df$Salary)
pop.stddev <- calc_pop_sd(df$Salary)
cat("population mean:", pop.mean, '\n')
cat("population standard deviation:", pop.stddev)
pop.stddev
calc_pop_sd <- function (vec) {
return sqrt(sum((df$Salary-mean(df$Salary))**2) / nrow(vec))
calc_pop_sd <- function (vec) {
sqrt(sum((vec-mean(vec))**2) / nrow(vec))
}
df         <- read.csv(file="baseball.csv", header=TRUE, sep=",")
df
K          <- nrow(df)
K
mean(df$Salary)
pop.mean   <- mean(df$Salary)
pop.stddev <- calc_pop_sd(df$Salary)
cat("population mean:", pop.mean, '\n')
cat("population standard deviation:", pop.stddev)
calc_pop_sd <- function (vec) {
print(sqrt(sum((vec-mean(vec))**2) / nrow(vec)))
}
df         <- read.csv(file="baseball.csv", header=TRUE, sep=",")
df
K          <- nrow(df)
K
mean(df$Salary)
pop.mean   <- mean(df$Salary)
pop.stddev <- calc_pop_sd(df$Salary)
calc_pop_sd <- function (vec) {
print(mean(vec))
print(sqrt(sum((vec-mean(vec))**2) / nrow(vec)))
}
pop.stddev <- calc_pop_sd(df$Salary)
calc_pop_sd <- function (vec) {
print(nrow(vec))
print(sqrt(sum((vec-mean(vec))**2) / nrow(vec)))
}
pop.stddev <- calc_pop_sd(df$Salary)
calc_pop_sd <- function (vec) {
print(length(vec))
print(sqrt(sum((vec-mean(vec))**2) / length(vec)))
}
pop.stddev <- calc_pop_sd(df$Salary)
calc_pop_sd <- function (vec) {
sqrt(sum((vec-mean(vec))**2) / length(vec))
}
df         <- read.csv(file="baseball.csv", header=TRUE, sep=",")
df
K          <- nrow(df)
K
mean(df$Salary)
pop.mean   <- mean(df$Salary)
pop.stddev <- calc_pop_sd(df$Salary)
cat("population mean:", pop.mean, '\n')
cat("population standard deviation:", pop.stddev)
calc_pop_sd <- function (vec) {
sqrt(sum((vec-mean(vec))**2) / length(vec))
}
df         <- read.csv(file="baseball.csv", header=TRUE, sep=",")
K          <- nrow(df)
pop.mean   <- mean(df$Salary)
pop.stddev <- calc_pop_sd(df$Salary)
cat("population mean:", pop.mean, '\n')
cat("population standard deviation:", pop.stddev)
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma.sq <- sd(Y.star)
cat("std dev of the sample mean (formulaic):",sqrt(sample.sigma.sq / n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma <- sd(Y.star)
cat("std dev of the sample mean (formulaic):",sample.sigma / sqrt(n)))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma <- sd(Y.star)
cat("std dev of the sample mean (formulaic):",sample.sigma / sqrt(n))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma <- sd(Y.star)
cat("std dev of the sample mean (formulaic):",sample.sigma / sqrt(n))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
cdf.2 <- dbinom(0, size=size, prob=prob) +
+ dbinom(1, size=size, prob=prob) +
+ dbinom(2, size=size, prob=prob)
cdf.2
sqrt(cdf.2 * (1 - cdf.2) / 100)
num.trails <- 7
n <- 40
prob <- 0.1
samples <- rbinom(n, num.trails, prob)
plot(ecdf(samples))
num.trails <- 7
n <- 40
prob <- 0.1
samples <- dbinom(n, num.trails, prob)
plot(ecdf(samples))
num.trails <- 7
n <- 40
prob <- 0.1
samples <- rinom(n, num.trails, prob)
num.trails <- 7
n <- 40
prob <- 0.1
samples <- rbinom(n, num.trails, prob)
plot(ecdf(samples))
ecdf.2s <- replicate(R, {
samples <- rbinom(n, num.trails, prob)
ecdf(samples)(2)
})
sd(ecdf.2s)
cdf.2 <- dbinom(0, size=size, prob=prob) +
+ dbinom(1, size=size, prob=prob) +
+ dbinom(2, size=size, prob=prob)
sqrt(cdf.2 * (1 - cdf.2) / 100)
cdf.2 <- dbinom(0, size=size, prob=prob) +
+ dbinom(1, size=size, prob=prob) +
+ dbinom(2, size=size, prob=prob)
sqrt(cdf.2 * (1 - cdf.2) / 100)
cdf.2 <- dbinom(0, size=size, prob=prob) +
+ dbinom(1, size=size, prob=prob) +
+ dbinom(2, size=size, prob=prob)
sqrt(cdf.2 * (1 - cdf.2) / n)
cdf.2 <- dbinom(0, size=size, prob=prob) +
+ dbinom(1, size=size, prob=prob) +
+ dbinom(2, size=size, prob=prob)
sqrt(cdf.2 * (1 - cdf.2) / n)
cdf.2 <- dbinom(0, size=size, prob=prob) +
+ dbinom(1, size=size, prob=prob) +
+ dbinom(2, size=size, prob=prob)
sqrt(cdf.2 * (1 - cdf.2) / n)
cdf.2 <- dbinom(0, size=size, prob=prob) +
+ dbinom(1, size=size, prob=prob) +
+ dbinom(2, size=size, prob=prob)
sqrt(cdf.2 * (1 - cdf.2) / 100)
cdf.2 <- dbinom(0, size=size, prob=prob) +
+ dbinom(1, size=size, prob=prob) +
+ dbinom(2, size=size, prob=prob)
sqrt(cdf.2 * (1 - cdf.2) / 100)
cdf.2 <- dbinom(0, num.trails, prob) +
+ dbinom(1, num.trails, prob) +
+ dbinom(2, num.trails, prob)
sqrt(cdf.2 * (1 - cdf.2) / 100)
cdf.2 <- dbinom(0, num.trails, prob) +
+ dbinom(1, num.trails, prob) +
+ dbinom(2, num.trails, prob)
sqrt(cdf.2 * (1 - cdf.2) / n)
calc_sample_stddev_of_sample_avg <- function(size) {
sample_avgs <- calc_sample_avgs(size)
sqrt(sum((sample_avgs-mean(sample_avgs)) ** 2) / (R-1) )
}
cat("a sample standard deviation of a sample average using 100 sample averages with sample size of each...",'\n')
cat("n=5: ", calc_sample_stddev_of_sample_avg(5),'\n')
cat("n=20:", calc_sample_stddev_of_sample_avg(20),'\n')
cat("n=80:", calc_sample_stddev_of_sample_avg(80))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma <- sd(Y.star)
cat("std dev of the sample mean (formulaic):",sample.sigma / sqrt(n))
n = 20
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma <- sd(Y.star)
cat("std dev of the sample mean (formulaic):",sample.sigma / sqrt(n))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
R = 5000
calc_bootsrap_stddev <- function (sample) {
bootstrap <- sample(Y.star,size=n,replace=T)
mean(bootstrap)
}
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
bootsrap.stddevs <- replicate(R, calc_bootsrap_stddev(80))
cat("variance of the sample mean (bootstrap):",mean(bootsrap.stddevs))
n = 80
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma <- sd(Y.star)
cat("std dev of the sample mean (formulaic):",sample.sigma / sqrt(n))
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
n = 80
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma <- sd(Y.star)
cat("std dev of the sample mean (formulaic):",sample.sigma / sqrt(n),'\n')
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
n = 80
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma <- sd(Y.star)
cat("std dev of the sample mean (formulaic):",sample.sigma / sqrt(n),'\n')
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
n = 80
Y.star = sample(df$Salary,size=n,replace=T)
sample.sigma <- sd(Y.star)
cat("std dev of the sample mean (formulaic):",sample.sigma / sqrt(n),'\n')
bootsrap.means <- replicate(R, calc_bootsrap_stddev(20))
cat("variance of the sample mean (bootstrap):",sd(bootsrap.means))
num.trails <- 7
n <- 40
prob <- 0.1
samples <- rbinom(n, num.trails, prob)
plot(ecdf(samples))
ecdf.2s <- replicate(R, {
samples <- rbinom(n, num.trails, prob)
ecdf(samples)(2)
})
cat("standard deviation of Fˆn(2)",sd(ecdf.2s))
cdf.2 <- dbinom(0, num.trails, prob) +
+ dbinom(1, num.trails, prob) +
+ dbinom(2, num.trails, prob)
cat("standard deviation of sqrt of Var(Fˆn(2))",sqrt(cdf.2 * (1 - cdf.2) / n))
ecdf(c(1,2,3,4))
plot(ecdf(c(1,2,3,4)))
source('~/.active-rstudio-document', echo=TRUE)
